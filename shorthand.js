const x = 50;
const y = 100;
const obj = {
    x: x,
    y: y
};

// shortchut
const obj1 = { x, y }

const name = 'Solimullah';
const area = 'Dhaka';

const nobab = {
    name: name,
    area: area
}

const nobab2 = { name, area };